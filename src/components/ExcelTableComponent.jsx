import React from 'react';

const ExcelTableComponent = ({ label, name }) => {
    return (
        <div>
            <label>{label} :</label>
        </div>
    );
};

export default ExcelTableComponent;
